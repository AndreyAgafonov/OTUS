[{{ master_hostname }}.{{ domain }}]
description =  "PostgreSQL Database Backup"
conninfo = host={{ master_hostname }}.{{ domain }} user={{ barman.username }} dbname=postgres
streaming_conninfo = host={{ master_hostname }}.{{ domain }} user={{ barman.streaming.username }} dbname=postgres
backup_method = postgres
streaming_archiver = on
slot_name = {{ barman.slot }}
archiver = on
